<ui1:AirAlarmWindow
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2007/xaml"
    xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    xmlns:ui1="clr-namespace:Content.Client.Atmos.Monitor.UI"
    MinSize="500 500" SetSize="700 550" Resizable="False" MouseFilter="Stop">

    <PanelContainer Name="Background" StyleClasses="PdaBackgroundRect" ModulateSelfOverride="#22232e"/>
    <PanelContainer Name="Border" StyleClasses="PdaBorderRect"/>

    <BoxContainer Orientation="Vertical" HorizontalExpand="True">

        <!-- Крестик для выхода -->
        <BoxContainer SetHeight="26" Margin="15 5" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextureButton Name="CloseButton" StyleClasses="windowCloseButton" Modulate="#646464" VerticalAlignment="Center" Margin="0 4 0 0"/>
        </BoxContainer>

        <!-- Содержимое -->
        <Control Margin="15 0 15 15" RectClipContent="True" VerticalExpand="true" HorizontalExpand="True">

            <!-- Контрастный цвет -->
            <PanelContainer ModulateSelfOverride="#FFFFFF" StyleClasses="PdaBackground">
                <PanelContainer.PanelOverride>
                    <gfx:StyleBoxFlat BackgroundColor="#050c0d" BorderColor="#1d1e27" BorderThickness="3"/>
                </PanelContainer.PanelOverride>
            </PanelContainer>

            <BoxContainer Margin="10 10 10 10" SeparationOverride="10" Orientation="Vertical" HorizontalExpand="True">
                <!-- Status (pressure, temperature, alarm state, device total, address, etc) -->
                <BoxContainer Orientation="Horizontal" SeparationOverride="40" Margin="0 0 0 2">
                    <!-- Left column (view of entity) -->
                    <PanelContainer Visible="False" Margin="2 0 6 0" StyleClasses="Inset" VerticalAlignment="Center" VerticalExpand="True">
                        <SpriteView Name="EntityView" OverrideDirection="South" Scale="2 2" SetSize="64 64"/>
                    </PanelContainer>
                    <!-- Center column (pressure, temperature, alarm state) -->
                    <BoxContainer Modulate="#4bb2b5" Orientation="Horizontal" SeparationOverride="10" HorizontalExpand="True" Margin="0 0 6 0">
                        <BoxContainer Orientation="Vertical" Margin="0 0 2 0" HorizontalExpand="True">
                            <Label Text="{Loc 'air-alarm-ui-window-pressure-label'}" />
                            <Label Text="{Loc 'air-alarm-ui-window-temperature-label'}" />
                            <Label Text="{Loc 'air-alarm-ui-window-alarm-state-label'}" />
                        </BoxContainer>
                        <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                            <RichTextLabel Name="CPressureLabel" />
                            <RichTextLabel Name="CTemperatureLabel" />
                            <RichTextLabel Name="CStatusLabel" />
                        </BoxContainer>
                    </BoxContainer>
                    <!-- Right column (address, device total) -->
                    <BoxContainer Modulate="#4bb2b5" Orientation="Vertical">
                        <BoxContainer Orientation="Horizontal" Margin="0 0 2 0" HorizontalExpand="True">
                            <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                                <Label Text="{Loc 'air-alarm-ui-window-address-label'}" />
                                <Label Text="{Loc 'air-alarm-ui-window-device-count-label'}" />
                            </BoxContainer>
                            <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                                <RichTextLabel Name="CDeviceAddress" HorizontalAlignment="Right" />
                                <RichTextLabel Name="CDeviceTotal" HorizontalAlignment="Right" />
                            </BoxContainer>
                        </BoxContainer>
                        <ui:MonotoneButton StyleClasses="OpenBoth" Name="CResyncButton" Text="{Loc 'air-alarm-ui-window-resync-devices-label'}" HorizontalExpand="True" />
                    </BoxContainer>
                </BoxContainer>
                <!-- Gas/Device Data -->
                <TabContainer StyleClasses="CyanTab" Name="CTabContainer" VerticalExpand="True" Margin="0 0 0 2">
                    <!-- Окантовка вокруг карты -->
                    <TabContainer.PanelStyleBoxOverride>
                        <gfx:StyleBoxFlat BorderColor="#4bb2b5" BorderThickness="2" />
                    </TabContainer.PanelStyleBoxOverride>
                    <!-- Vent devices -->
                    <PanelContainer VerticalExpand="True">
                        <ScrollContainer>
                            <BoxContainer Modulate="#4bb2b5" Name="CVentContainer" Orientation="Vertical"/>
                        </ScrollContainer>
                    </PanelContainer>
                    <!-- Scrubber devices -->
                    <PanelContainer VerticalExpand="True">
                        <ScrollContainer>
                            <BoxContainer Modulate="#4bb2b5" Name="CScrubberContainer" Orientation="Vertical"/>
                        </ScrollContainer>
                    </PanelContainer>
                    <!-- Sensors -->
                    <PanelContainer VerticalExpand="True">
                        <ScrollContainer>
                            <BoxContainer Modulate="#4bb2b5" Name="CSensorContainer" Orientation="Vertical"/>
                        </ScrollContainer>
                    </PanelContainer>
                </TabContainer>
                <!-- Mode buttons -->
                <BoxContainer Modulate="#4bb2b5" Orientation="Horizontal" SeparationOverride="10">

                    <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
                        <Label Text="{Loc 'air-alarm-ui-window-mode-label'}" Margin="0 0 2 0" />
                        <OptionButton Name="CModeButton" StyleClasses="OpenBoth"/>
                        <ui:StripeBack Name="CModeSelectLocked">
                            <RichTextLabel Text="{Loc 'air-alarm-ui-window-mode-select-locked-label'}"
                                           HorizontalAlignment="Center" />
                        </ui:StripeBack>
                    </BoxContainer>

                    <CheckBox  HorizontalAlignment="Right" HorizontalExpand="True" Name="AutoModeCheckBox" Text="{Loc 'air-alarm-ui-window-auto-mode-label'}" />
                </BoxContainer>
            </BoxContainer>
        </Control>
    </BoxContainer>
</ui1:AirAlarmWindow>
